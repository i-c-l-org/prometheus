import type { NodePath } from '@babel/traverse';
import type { Node } from '@babel/types';
import type { ContextoExecucao, Ocorrencia } from '../../types/index.js';
export declare const analistaSeguranca: {
    nome: string;
    categoria: string;
    descricao: string;
    limites: {
        MAX_HARDCODED_SECRETS: number;
        MAX_EVAL_USAGE: number;
        MAX_INNERHTML: number;
        MAX_WEAK_CRYPTO: number;
        MAX_UNSAFE_REGEX: number;
        MAX_PATH_TRAVERSAL: number;
        MAX_SQL_INJECTION: number;
        MAX_COMMAND_INJECTION: number;
        MAX_UNHANDLED_ASYNC: number;
        MAX_IGNORAR_TESTES: boolean;
    };
    test: (relPath: string) => boolean;
    aplicar: (src: string, relPath: string, ast: NodePath<Node> | null, _fullPath?: string, contexto?: ContextoExecucao) => Promise<Ocorrencia[] | null>;
};
//# sourceMappingURL=detector-seguranca.d.ts.map