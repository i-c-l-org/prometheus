import type { NodePath } from '@babel/traverse';
import type { Node } from '@babel/types';
import type { ContextoExecucao, Ocorrencia } from '../../types/index.js';
export declare const analistaDesempenho: {
    nome: string;
    categoria: string;
    descricao: string;
    limites: {
        MAX_LOOPS_ANNIDED: number;
        MAX_BLOCKING_OPS: number;
        MAX_MEMORY_LEAKS: number;
        MAX_N_PLUS_ONE: number;
        MAX_INEFFICIENT_SPREAD: number;
        MAX_LARGE_BUNDLE: number;
        MAX_CONSOLE_LOG: number;
        MAX_IGNORAR_TESTES: boolean;
    };
    test: (relPath: string) => boolean;
    aplicar: (src: string, relPath: string, ast: NodePath<Node> | null, _fullPath?: string, _contexto?: ContextoExecucao) => Promise<Ocorrencia[] | null>;
};
//# sourceMappingURL=detector-performance.d.ts.map